import { collection, getDocs, getFirestore } from "firebase/firestore";
import { APP_FIREBASE } from "@/Config/firebase/firebase.config";

const updateColletionHook = () => {
  return;
};

export default updateColletionHook;
